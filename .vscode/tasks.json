{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "win_build",
            "type": "shell",
            "command": "mingw32-g++ main.cpp -o ${workspaceFolder}/main",
        },
        {
            "label": "win_run",
            "type": "shell",
            "command": "${workspaceFolder}/main",
        }
    ]
}